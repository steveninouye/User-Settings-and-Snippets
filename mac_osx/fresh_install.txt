# install homebrew
https://brew.sh/

# install important applications
brew install wget

# install node JS
brew install npm
sudo npm install -g n
sudo n latest

# install fonts
brew tap caskroom/fonts
brew cask install font-fira-code
brew cask install font-inconsolata

# create .bash_profile in home (~) directory
## write inside
PS1="\e[38;5;41m\]$ \d \A \h[\u]: \w\n$ \e[0m\]"
alias gc="git commit -m "
alias ga="git add ."
alias gp="git push"
export PATH=${PATH}:/usr/local/mysql/bin/
export CLICOLOR=1
export LSCOLORS=fxhecxdxbxegedabagacad
source ~/.bashrc

# customize terminal color (http://www.marinamele.com/2014/05/customize-colors-of-your-terminal-in-mac-os-x.html)
## terminal font
Monaco 12 pt

## terminal preferences
Display ANSI colors  # check the box

# execute .bash_profile to update terminal settings
source .bash_profile

///////////////// backing up mac settings

# bundle all applications
brew bundle

# bundle vs code extensions
code --list-extensions | xargs -L 1 echo code --install-extension
